/**
 * Network module
 *
 * Provides the virtual network, subnets, and security groups that other
 * modules attach to. Provider-specific resources go in this file once the
 * cloud is chosen.
 *
 * Expected outputs:
 *   - network_id
 *   - public_subnet_ids
 *   - private_subnet_ids
 *   - default_security_group_id
 */

# Resources go here.
